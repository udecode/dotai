{
  "beforeStart": [
    {
      "tag": "MANDATORY-FIRST-RESPONSE",
      "header": "BEFORE responding to ANY user message:",
      "instructions": [
        "BEFORE doing anything else: Create TodoWrite with todos below as first items and mark first as in_progress",
        "Work through each todo: mark in_progress → complete the check → mark completed",
        "If a skill has a checklist, create TodoWrite todos for EACH item (one todo per checklist item)",
        "NEVER work through checklists mentally, skip creating todos, or batch multiple items"
      ],
      "todos": [
        "Skill analysis: Check for rationalizations ('simple question', 'just checking files', 'gather info first', 'don't need formal skill', 'I remember this', 'doesn't count as task', 'overkill', 'one thing first') → STOP if rationalizing → Analyze skills and announce: '[Skills checked: X available, Y match]' OR '[Using skill: name] to [purpose]'",
        "Execute skill: If skill matches, load with Skill tool (check if already loaded this session) and follow exactly - if even 1% chance skill applies you MUST use it or you will fail"
      ]
    }
  ],
  "beforeComplete": [
    {
      "tag": "VERIFICATION-CHECKLIST",
      "header": "Before claiming work is complete, fixed, or passing - NO completion claims without FRESH verification evidence:",
      "instructions": [
        "BEFORE claiming work complete: Create TodoWrite with todos below and mark first as in_progress",
        "Work through EACH verification todo: mark in_progress → complete check → mark completed",
        "NEVER claim completion without completing ALL verification todos",
        "We manually start session with `npm run logs && npm run typecheck:watch` as Background Bash Shell - read output when checking errors",
        "Run `npm run typecheck` only when Background Bash Shells are not visible/running"
      ],
      "todos": [
        "Check no TypeScript `any` used (pause and ask user if `any` seems required)",
        "Read typecheck:watch output OR run `npm run typecheck` - verify passes",
        "Run `npm run lint:fix` - verify passes",
        "Verify no git commits made unless user explicitly asked",
        "NEVER run `npm run dev` or `npm run build` unless explicitly asked"
      ]
    }
  ]
}
